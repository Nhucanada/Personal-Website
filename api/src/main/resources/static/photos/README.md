Store photography assets in this directory.

- Put final images in logical subfolders (for example `travel/`, `street/`, `portraits/`).
- Keep placeholder assets in `placeholders/` until real photos are available.
- Reference files through `imagePath` in `data/photography-photos.json` using paths like `/photos/travel/frame-01.jpg`.
